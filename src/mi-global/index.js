import * as React from 'react';
import * as ReactDOM from 'react-dom';
import * as reactRouterDOM from 'react-router-dom';


window.__miReact = React;
window.__miReactDOM = ReactDOM;
window.__miReactRouterDOM = reactRouterDOM;
